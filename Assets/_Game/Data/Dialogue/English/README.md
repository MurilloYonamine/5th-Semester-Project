# English Dialogue

This folder stores the English versions of the dialogue and note text files used by the game.

Each file mirrors the Portuguese file name so the localization lookup can stay consistent.

Documents are split into one file per note or report, using the same note id across languages (for example, `N1.txt`, `N2.txt`).

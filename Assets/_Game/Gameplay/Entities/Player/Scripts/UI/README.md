# Player UI

This folder contains player-facing UI controllers.

## Components
- `PlayerSprintBarUI` - updates the sprint/stamina bar based on player movement.
- `PlayerStateUIController` - toggles crosshair and stamina UI visibility based on `GameStateChangedEvent`.
